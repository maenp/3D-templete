# 开发规范

## 1. 启动流程

`launch` 快速启动场景 → 完成资源 / bundle 加载 → 进入 `app` 场景 → 打开 `screen` 页面。

app 场景只负责页面入口，例如 `UIJelly.openView()`。具体页面的 UI、节点和逻辑放在对应预制体与脚本中。

## 2. 目录与命名

| 类型 | 预制体位置 | 脚本位置 | 基类 |
| --- | --- | --- | --- |
| 全屏页面 | `assets/modules/app/screen/jelly.prefab` | `assets/scripts/modules/app/UIJelly.ts` | `UIScreen` |
| 弹窗 | `assets/modules/app/resultPopup.prefab` | `assets/scripts/modules/app/UIResultPopup.ts` | `UIWindow` |

- 预制体和根节点使用相同名称，采用小驼峰，如 `jelly`、`resultPopup`。
- 脚本文件、类名和 `@ccclass` 名称一致，使用 `UI` + 预制体名称首字母大写。
- `prefabPath` 格式为 `bundle名#预制体相对路径`，不带 `.prefab`，例如 `app#screen/jelly`、`app#resultPopup`。
- 现有 `UITestPopup` 等是历史示例；新页面按以上对应关系命名。

## 3. 创建全屏页面

1. 在 Cocos Creator 中创建页面根节点，添加 `UITransform`，设计尺寸参考项目的 `750 × 1624`。
2. 在根节点下创建 Label、Button、Sprite 等原生 UI，设置布局、尺寸和图片。
3. 创建对应的 `UIxxx.ts`，继承 `UIScreen`，挂载到预制体**根节点**。
4. 使用 `@property` 声明引用，在编辑器属性检查器中拖入对应节点或组件。
5. 将根节点拖到 `assets/modules/app/screen` 保存为预制体，确认脚本和引用已保存。
6. 通过 `UIxxx.openView()` 打开，由框架负责加载、全屏适配及挂载到 `UI_SCREEN`。

```ts
import { _decorator, Label } from 'cc';
import { UIScreen } from '../../frame/core/UIForm';
const { ccclass, property } = _decorator;

@ccclass('UIExample')
export default class UIExample extends UIScreen {
    static prefabPath = 'app#screen/example';

    @property(Label)
    title: Label = null!;

    onShow(params?: { title?: string }) {
        this.title.string = params?.title ?? '示例页面';
    }
}
```

调用：`UIExample.openView({ title: '欢迎' })`。页面内调用 `this.closeSelf()` 返回上一页；第一页没有可返回页面。

## 4. 创建 popup 弹窗

1. 在编辑器创建弹窗根节点和面板、文本、关闭按钮等子节点。
2. 保存到 `assets/modules/app/resultPopup.prefab`，根节点挂载 `UIResultPopup.ts`。
3. 脚本继承 `UIWindow`，设置 `prefabPath`，按页面相同方式绑定组件。
4. 在关闭按钮的 Button → Click Events 中，拖入预制体根节点，选择脚本的 `onCloseClick`。

```ts
import { _decorator } from 'cc';
import { UIWindow } from '../../frame/core/UIForm';
const { ccclass } = _decorator;

@ccclass('UIResultPopup')
export default class UIResultPopup extends UIWindow {
    static prefabPath = 'app#resultPopup';
    clickMaskClose = true;

    onShow(params?: { score: number }) {
        // 更新已绑定的分数文本等内容。
    }

    onCloseClick() {
        this.closeSelf();
    }
}
```

调用：`UIResultPopup.openView({ score: 100 })`。框架负责 `UI_POPUP` 层、遮罩和默认缩放动画；设置 `clickMaskClose = false` 可禁止点击遮罩关闭。

## 5. 脚本与验证

- 静态 UI 在预制体中创建；脚本负责逻辑、数据更新和必要的运行时对象，不用代码搭建整套按钮与文本。
- `onShow(params)` 接收打开参数、初始化或刷新页面；重复显示时避免重复注册事件。
- `onHide()` 停止页面的定时任务和动画；`onDestroy()` 解绑全局输入 / 事件，释放自行创建的资源。
- 子节点可能先于根脚本销毁，`onDestroy()` 不要直接访问已销毁的按钮或节点；节点自身事件由 Cocos 随节点清理。
- 普通页面和弹窗沿用框架默认的关闭销毁行为；只有确实需要常驻时才设置 `permanentNode = true`。
- 页面适配使用框架及 Widget，不随意更改 launch 的全局分辨率设置。
- 修改后先切离再切回 Cocos Creator，等待资源导入和脚本编译，再刷新浏览器预览。检查打开、关闭、重开及控制台错误。
